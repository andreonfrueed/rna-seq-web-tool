# RNA 分析网页 · 使用说明

一个给实验室用的 **RNA 测序分析工具**：网页界面，上传测序数据 → 选人/小鼠 → 自动跑完整个分析（比对 → 数基因 → 找差异 → 富集）→ 下载结果。

---

## 一、安装（只用装一次）

**要求**：Windows 10/11 电脑，内存建议 **16GB 以上**（小鼠数据 16GB 够，人数据建议 32GB），硬盘剩余 **50GB 以上**。

1. 把整个文件夹拷贝到你的电脑（U盘/网盘都行）
2. **双击 `一键安装.bat`**
   - 如果提示"没有 WSL"，按窗口里的提示先装 WSL（一条命令 + 重启），再双击一次
   - 安装全程自动，约 30-60 分钟，**中途别关窗口**
   - 装完它会说"安装结束"

## 二、开始用（每次分析）

1. **双击 `打开分析网页.bat`** → 浏览器自动打开网页（http://localhost:8501）
2. 网页里有 5 个页面：

| 页面 | 干什么 |
|---|---|
| 🔍 环境体检 | 检查软件装好没（不用管，装好后都绿） |
| 📤 数据与参考文件 | **选人/小鼠** → 点"下载参考文件"（只下第一次）→ 传测序数据 |
| 👥 分组与参数 | 把每个样本勾到"对照组/处理组"，调阈值（默认就行）。**每个样本都要勾**，每组至少 2 个样本、建议 3 个以上 |
| ▶️ 运行分析 | 起个名字 → 点"开始分析" → 显示实时进度（想中途停就点"🛑 停止"） |
| 📥 结果下载 | 图可以直接预览；全部结果表格和图，单个下载或打包 ZIP |

## 三、传数据（重要）

- **小文件**：网页里直接拖进去（单个最大 2GB）
- **大文件（超过 1GB 或一次很多个，推荐）**：双击 `打开上传文件夹.bat`，把 fastq 文件拖进弹出的文件夹，然后回网页点"**扫描已有文件**"
- **文件命名必须是**：`样本名_R1.fastq.gz` 和 `样本名_R2.fastq.gz`（配对），程序才认
- 识别出来后**勾选本次要分析的样本**——上次的旧样本不想用就去掉勾，或在下方"管理上传文件夹"里直接删除，避免混进新分析
- 重新上传同名文件会**覆盖**旧文件，改错数据重传不用担心

## 四、注意事项

- **分析要跑很久**（几小时到十几小时）。跑之前**去电脑设置里把"睡眠"改成"从不"**（否则电脑睡着会把分析中断）。别合笔记本盖子。
- **比对引擎默认是 HISAT2**（省内存、稳定）。你电脑内存够大（32GB+）想用 STAR 也可以，在"运行分析"页下拉切换。
- **差异分析默认是 DESeq2**（论文标准做法，输出 log2FC/padj，并自动生成 VST 标准化矩阵和 VST 热图）。如果 R/DESeq2 没装成功，可以在"运行分析"页切回旧引擎 pydiffexpress 应急。
- 开跑前网页会**自动检查磁盘空间和文件完整性**，发现问题会拦下来并告诉你怎么处理——不用等几小时后才发现。
- 跑完几次分析后磁盘会紧张，到"结果下载"页点"**清理中间文件**"释放空间（结果表格和图片都会保留）。
- 数据只在你自己的电脑上算，**不上传任何服务器**。

## 五、结果是什么

跑完后在"结果下载"页能拿到：
- **表达矩阵**（每个基因在每个样本的读数）
- **差异基因表**（上调/下调、倍数、P 值，有基因名）
- **DESeq2 差异表与 VST 矩阵**：论文级 log2FC/padj；`VST_normalized_counts.csv` 是标准化的表达矩阵，论文级热图/下游分析请用它（RPKM 表保留供快速浏览）
- **火山图、热图、PCA 图、聚类图**等
- **GO/KEGG 富集**：每个比较的**上调、下调基因分开做**（哪些通路被激活、哪些被抑制，如炎症、免疫）；点"**一键下载全部富集结果**"可整包下下来，不用再逐个文件点
- **比较方向怎么看**：比较写作「X vs Y」（如 LPS vs C），差异倍数为正 = **X 组表达更高**；富集结果的文件夹会直接标明方向（如「LPS高于C」就是 LPS 组更高的基因）。开跑前"运行分析"页会列出本次所有比较和方向，记得扫一眼。旧版结果里的 up/down 目录：up = 比较名中前一组更高。

## 六、出问题怎么办

- 安装/运行报错：把红色报错信息截图；分析失败时"运行分析"页有"**下载运行日志**"按钮，把日志一并发给懂技术的人
- 分析卡住/参数填错了想重来：到"运行分析"页点"**🛑 停止**"，再重新开跑
- 内存不够（分析中途死了）：检查是不是电脑睡眠了，或把"内存"参数调低一点
- 网页打不开：先双击 `打开分析网页.bat` 等 30 秒，浏览器再开 http://localhost:8501；等 2 分钟还打不开，脚本会提示你去 WSL 里看 `cat /tmp/rna_web.log`
